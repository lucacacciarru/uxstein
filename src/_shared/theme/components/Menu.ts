//Todo: Complete
import type { ComponentStyleConfig } from "@chakra-ui/react";

export const Menu: ComponentStyleConfig = {
  defaultProps: {
    list: {
      _hover: {
        bg: "brand.primary",
      },
    },
  },
};
