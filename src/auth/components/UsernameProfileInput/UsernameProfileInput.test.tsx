import { render } from '../../../_shared/testConfig/customRender';
import { UsernameProfileInput } from './UsernameProfileInput';

describe('UsernameProfileInput component', () => {
  test('Should be rendered', () => {
    render(<UsernameProfileInput />);
  });
});
