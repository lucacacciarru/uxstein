import { render } from '../../../_shared/testConfig/customRender';
import { AvatarProfileInput } from './AvatarProfileInput';

describe('AvatarProfileInput', () => {
  test('should be rendered', () => {
    render(<AvatarProfileInput />);
  });
});
