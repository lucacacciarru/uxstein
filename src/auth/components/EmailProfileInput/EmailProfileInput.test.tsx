import { render } from '../../../_shared/testConfig/customRender';
import { EmailProfileInput } from './EmailProfileInput';

describe('EmailProfileInput component', () => {
  test('Should be rendered', () => {
    render(<EmailProfileInput />);
  });
});
