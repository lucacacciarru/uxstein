import { render } from '../../../_shared/testConfig/customRender';
import { SaveAsTemplateButton } from './SaveAsTemplateButton';

describe('SaveAsTemplateButton component', () => {
  test('should be rendered', () => {
    render(<SaveAsTemplateButton />);
  });
});
