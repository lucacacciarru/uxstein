import { TranslationKey } from '../../../_shared/types/i18n';
import { DraggableBlockType } from '../../components/AddTab/DraggableBlock';

export const draggableBlocksLabelsMap: Record<DraggableBlockType, TranslationKey> = {
    text: 'auth',
    image: 'auth',
    "image-list": 'auth',
    "text-list": 'auth',
    checkbox: 'auth',
    progress: 'auth',
    rate: 'auth'
}